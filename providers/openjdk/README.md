The `Test.java` program requires the patch at https://github.com/openjdk/jdk/pull/24969.

Run `gen.sh` inside this directory to generate the ZIP file.

Run `check.sh [provider_name]` inside this directory for interop testing.
