# PRIVATE KEY REMOVED — see security notice
# This file previously contained a software-test-environment signature private key
# that was committed to the public repository in error.
#
# The key was regenerated and the exposed key is now invalid for any Sanctum
# production, staging, or identity purpose — it was a lab-only key generated
# on the CYGNUS evidence node (cyadmin@cygnus).
#
# If you need to regenerate test keys, use:
#   python3 tools/generate_sanctum_provider.py --output artifacts_certs_r5/
#
# DO NOT commit private keys to this repository. Add to .gitignore.
# PATENT PENDING — Sanctum SecOps LLC

